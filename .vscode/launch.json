{{

    "version": "0.2.0",    "version": "0.2.0",

    "configurations": [    "configurations": [

        {        {

            "name": "Debug chordTest (CMake)",            "name": "Run Active File (g++, build and debug)",

            "type": "cppdbg",            "type": "cppdbg",

            "request": "launch",            "request": "launch",

            "program": "${workspaceFolder}/build/examples/chordTest.exe",            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",

            "cwd": "${workspaceFolder}",            "preLaunchTask": "build active file",

            "stopAtEntry": false        }

        }    ]

    ]}

}
